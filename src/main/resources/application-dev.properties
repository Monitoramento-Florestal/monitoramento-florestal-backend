# Perfil local de desenvolvimento.
# Nao use este valor em producao.
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5434/arbor_db?sslmode=disable}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:arbor_user}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:arbor_password}
jwt.secret=dev-secret-apenas-local-com-32-caracteres

arbor.bootstrap.admin.name=${ARBOR_BOOTSTRAP_ADMIN_NAME:Admin Dev}
arbor.bootstrap.admin.email=${ARBOR_BOOTSTRAP_ADMIN_EMAIL:admin-dev@arbor.local}
arbor.bootstrap.admin.password=${ARBOR_BOOTSTRAP_ADMIN_PASSWORD:DevAdmin123!}
arbor.bootstrap.admin.role=${ARBOR_BOOTSTRAP_ADMIN_ROLE:ADMINISTRADOR}
